#!/bin/sh
cat << END
/*
 * defaults.h 
 * Automatically generated
 */
#define OPBX_CONFIG_DIR "${INSTALL_PATH}${ASTETCDIR}"
#define OPBX_RUN_DIR    "${INSTALL_PATH}${ASTVARRUNDIR}"
#define OPBX_SOCKET     "${INSTALL_PATH}${ASTVARRUNDIR}/openpbx.ctl"
#define OPBX_PID        "${INSTALL_PATH}${ASTVARRUNDIR}/openpbx.pid"
#define OPBX_MODULE_DIR "${INSTALL_PATH}${MODULES_DIR}"
#define OPBX_SPOOL_DIR  "${INSTALL_PATH}${ASTSPOOLDIR}"
#define OPBX_VAR_DIR    "${INSTALL_PATH}${ASTVARLIBDIR}"
#define OPBX_LOG_DIR    "${INSTALL_PATH}${ASTLOGDIR}"
#define OPBX_AGI_DIR    "${INSTALL_PATH}${AGI_DIR}"
#define OPBX_KEY_DIR    "${INSTALL_PATH}${ASTVARLIBDIR}/keys"
#define OPBX_DB         "${INSTALL_PATH}${ASTVARLIBDIR}/astdb"
#define OPBX_TMP_DIR    "${INSTALL_PATH}${ASTSPOOLDIR}/tmp"

#define OPBX_CONFIG_FILE "${INSTALL_PATH}${ASTCONFPATH}"

#define OPBX_SOUNDS     "${INSTALL_PATH}${ASTVARLIBDIR}/sounds"
#define OPBX_IMAGES     "${INSTALL_PATH}${ASTVARLIBDIR}/images"

END
