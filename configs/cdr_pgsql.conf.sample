; Sample OpenPBX config file for CDR logging to PostgresSQL

[global]
;hostname=localhost
;port=5432
;dbname=openpbx
;password=password
;user=postgres
;table=cdr		;SQL table where CDRs will be inserted
